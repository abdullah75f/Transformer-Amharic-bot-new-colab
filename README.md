# Transformer-Amharic-bot-new-colab
This Google Colab notebook contains the complete process for fine-tuning rasyosef/gpt2-small-amharic. It handles data loading, cleaning, tokenization, training via Hugging Face, and model saving


python3 -m venv venv
source venv/bin/activate
streamlit run chatbot_app.py --server.fileWatcherType none
pip install -r requirements.txt